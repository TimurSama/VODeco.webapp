# Руководство по системе попапов VODeco

## Обзор изменений

Система попапов была полностью переработана для улучшения организации кода, упрощения поддержки и создания красивой визуализации данных.

## Ключевые улучшения

### 1. Централизованное хранилище данных
Все тексты, описания и данные теперь находятся в одном файле: `js/data.js`

**Преимущества:**
- ✅ Легко редактировать контент без изменения HTML
- ✅ Поддержка мультиязычности
- ✅ Централизованное управление данными
- ✅ Нет дублирования информации

### 2. Улучшенная система попапов
Новый файл `js/popup-system.js` обрабатывает все попапы автоматически.

**Возможности:**
- 📊 Автоматическое создание таблиц из данных
- 📈 Визуализация данных (графики, диаграммы)
- 🎨 Красивое оформление с анимациями
- 📱 Адаптивный дизайн

### 3. Новые стили для попапов
Файл `styles/popup-styles.css` содержит стили для:
- Таблиц с данными
- Графиков и диаграмм
- Карточек и метрик
- Анимаций появления

## Как использовать

### Добавление нового попапа

1. **Добавьте данные в `js/data.js`:**

```javascript
const VODecoData = {
    // Ваша категория
    myCategory: {
        myItem: {
            title: 'Заголовок',
            details: {
                mainTitle: 'Основной заголовок',
                description: 'Описание',
                table: [
                    { column1: 'Значение1', column2: 'Значение2' }
                ]
            }
        }
    }
};
```

2. **Добавьте атрибут data-popup в HTML:**

```html
<div class="my-element" data-popup="myCategory.myItem">
    Нажмите для просмотра
</div>
```

### Типы визуализации данных

#### Таблицы
```javascript
details: {
    table: [
        { region: 'Азия', percentage: '45%', amount: '623.7 млн VOD' }
    ]
}
```

#### Метрики
```javascript
details: {
    metrics: [
        { label: 'Капитализация', value: '$1.8 млрд' }
    ]
}
```

#### Графики (горизонтальные бары)
```javascript
details: {
    regions: [
        { name: 'Азия', affected: '1.2 млрд человек', percentage: '54.5%' }
    ]
}
```

#### Круговые диаграммы
```javascript
details: {
    lossTypes: [
        { type: 'Утечки', loss: '25-30%' }
    ]
}
```

#### Списки
```javascript
details: {
    consequences: [
        'Пункт 1',
        'Пункт 2'
    ]
}
```

#### Карточки с данными
```javascript
details: {
    features: [
        { name: 'Название', desc: 'Описание' }
    ]
}
```

## Структура файлов

```
VODeco_webapp/
├── js/
│   ├── data.js              # Все данные приложения
│   ├── popup-system.js      # Система попапов
│   ├── main.js              # Основная логика
│   ├── animations.js        # Анимации
│   └── spider-web.js        # Фоновая анимация
├── styles/
│   ├── main.css            # Основные стили
│   ├── popup-styles.css    # Стили для попапов ⭐ НОВОЕ
│   ├── animations.css      # Анимации
│   └── responsive.css      # Адаптивность
└── index.html              # Главная страница
```

## Примеры использования

### Пример 1: Простая информационная карточка
```javascript
// В data.js
myInfo: {
    title: 'Моя информация',
    details: {
        mainTitle: 'Заголовок',
        description: 'Описание'
    }
}
```

```html
<!-- В HTML -->
<div data-popup="myInfo">Показать информацию</div>
```

### Пример 2: Таблица с данными
```javascript
// В data.js
projectsTable: {
    title: 'Проекты',
    details: {
        mainTitle: 'Список проектов',
        table: [
            { name: 'Проект 1', budget: '$1M', status: 'Активный' },
            { name: 'Проект 2', budget: '$2M', status: 'В разработке' }
        ]
    }
}
```

### Пример 3: Графики с данными
```javascript
// В data.js
statistics: {
    title: 'Статистика',
    details: {
        mainTitle: 'Распределение по регионам',
        regions: [
            { name: 'Европа', affected: '500 млн', percentage: '25%' },
            { name: 'Азия', affected: '1 млрд', percentage: '50%' }
        ]
    }
}
```

## Доступные типы данных для визуализации

| Тип | Ключ в data | Описание |
|-----|-------------|----------|
| Таблица | `table` | Создает таблицу с колонками |
| Метрики | `metrics` | Карточки с метриками |
| Секторы | `sectors` | Карточки секторов с ROI |
| Регионы | `regions` | График распределения по регионам |
| Загрязнение | `pollutionTypes` | Карточки типов загрязнения |
| Потери | `lossTypes` | Круговые диаграммы потерь |
| Распределение | `distribution` | График распределения |
| Разбивка | `breakdown` | Таблица разбивки |
| Источники | `sources` | Pie chart источников |
| Производство | `sectors` (производство) | График производства |
| Качество | `quality` | Горизонтальные бары качества |
| Участники | `types` | Карточки участников |
| География | `geography` | График географии |
| Списки | `consequences`, `categories`, etc. | Маркированные списки |
| Возможности | `features` | Сетка карточек возможностей |

## Кастомизация

### Изменение цветов
Откройте `styles/popup-styles.css` и измените CSS переменные:
```css
:root {
    --primary-color: #00b4d8;
    --accent-color: #90e0ef;
    --text-color: #ffffff;
}
```

### Добавление нового типа визуализации
1. Добавьте метод в `js/popup-system.js`:
```javascript
createMyCustomVisualization(data) {
    let html = '<div class="my-custom-viz">';
    // Ваш код генерации HTML
    html += '</div>';
    return html;
}
```

2. Добавьте стили в `styles/popup-styles.css`:
```css
.my-custom-viz {
    /* Ваши стили */
}
```

3. Используйте в методе `generateContent`:
```javascript
if (details.myCustomData) {
    html += this.createMyCustomVisualization(details.myCustomData);
}
```

## Советы по работе

1. **Группируйте связанные данные** - используйте иерархию в `data.js`
2. **Используйте понятные ключи** - `heroStats.waterOnEarth` лучше чем `hs.we`
3. **Добавляйте анимации** - они автоматически применяются через класс `.animate-item`
4. **Проверяйте адаптивность** - все попапы адаптивны на мобильных устройствах
5. **Используйте типы данных** - система автоматически определит как визуализировать данные

## Отладка

Если попап не открывается:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что ключ в `data-popup` соответствует структуре в `data.js`
3. Проверьте, что все файлы подключены в `index.html`

## Производительность

- Попапы создаются динамически только при открытии
- Используется делегирование событий для эффективной работы с большим количеством элементов
- Анимации оптимизированы через CSS transitions

## Будущие улучшения

- [ ] Интеграция с Chart.js для более сложных графиков
- [ ] Экспорт данных из попапов в PDF/Excel
- [ ] История просмотренных попапов
- [ ] Поиск по данным внутри попапов
- [ ] Сравнение данных между разными попапами

## Поддержка

Если возникли вопросы или нужна помощь, обратитесь к команде разработки VODeco.



